name: APK Android

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    name: Gerando APK Android
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
